import javax.swing.*;

public class VulnTab extends JPanel {

    public VulnTab() {
        JPanel mainPanel = new JPanel();
        add(mainPanel);
        JLabel label = new JLabel("VulnTab");
        mainPanel.add(label);
    }
}
