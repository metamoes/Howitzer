import javax.swing.*;

public class CrossReference extends JPanel {

    public CrossReference() {
        JPanel mainPanel = new JPanel();
        add(mainPanel);
        JLabel label = new JLabel("CrossReference");
        mainPanel.add(label);
    }
}
