import javax.swing.*;

public class Penetrate extends JPanel {

    public Penetrate() {
        JPanel mainPanel = new JPanel();
        add(mainPanel);
        JLabel label = new JLabel("Penetrate");
        mainPanel.add(label);
    }
}
