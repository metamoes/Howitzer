import javax.swing.*;

public class Reporting extends JPanel {

    public Reporting() {
        JPanel mainPanel = new JPanel();
        add(mainPanel);
        JLabel label = new JLabel("Reporting");
        mainPanel.add(label);
    }
}
