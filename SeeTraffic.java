import javax.swing.*;

public class SeeTraffic extends JPanel {

    public SeeTraffic() {
        JPanel mainPanel = new JPanel();
        add(mainPanel);
        JLabel label = new JLabel("SeeTraffic");
        mainPanel.add(label);
    }
}
